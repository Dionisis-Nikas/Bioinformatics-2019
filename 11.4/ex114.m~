clear 
clc
A = [0.9 0.1; 0.1 0.9];
B = [0.4 0.1 0.4 0.1; 0.2 0.3 0.2 0.3;]';
katastaseis = ['α','β'] 

x = nt2int('GGCT');
prob_init = [0.5; 0.5];
seq = [x]
[score,best_path] = viterbi(prob_init,A,B,x);
best_path = fliplr(best_path);
disp("Best score was: ",score)
for i=1:size(best_path),
    best_path(i) = katastaseis(best
end
disp("Best path was: ",score)
